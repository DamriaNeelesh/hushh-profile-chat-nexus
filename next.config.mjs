/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you have images from external domains, configure them here
  // images: {
  //   domains: ['example.com'],
  // },
  // You can add other Next.js configurations here as needed
};

export default nextConfig; 